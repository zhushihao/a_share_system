# 优化摘要
时间: 2026-07-01 00:40 CST
基于报告: SYSTEM_LOOP_CHECK_REPORT_2026-07-01_0030.md, UX_CHECK_REPORT_2026-07-01_0035.md

## 修复的问题

1. **StockDetail.tsx 重复 fetchSettings useEffect** → 删除第二个 useEffect → tsc 通过
2. **AIResearch.tsx contextData 获取后未注入 sendAIChat** → sendAIChat 增加 context_data 参数，后端 AIChatRequest/ai_chat 接收并返回 → tsc 通过
3. **AIResearch.tsx handleTemplateClick 未传 history** → 与 handleSend 保持一致传入 history → tsc 通过
4. **SignalItem.signal_type 缺少 HOLD** → 联合类型增加 'HOLD' → tsc 通过
5. **fetchHealth 未使用 apiClient** → 改为 apiClient.get('/health', { baseURL: '/api' }) → tsc 通过
6. **POST body 为 null（track/close/fetchAIContext）** → 前端发送 JSON Body，后端改为 Body(...) 接收 → tsc 通过
7. **404 页面过于简单** → App.tsx 增加 404 卡片和返回首页 Link → tsc 通过
8. **无全局 ErrorBoundary** → App.tsx 增加 ErrorBoundary 包裹 Routes → tsc 通过
9. **主题设置不生效** → Settings.tsx theme 变化时更新 document.documentElement classList → tsc 通过
10. **回测 ResultCard 颜色与 A 股习惯相反** → Backtest.tsx / StrategyEditor.tsx 正收益 red-500、负收益 emerald-600 → tsc 通过
11. **部分页面组件未使用 React.memo** → Signals/Backtest/AIResearch/StrategyEditor/DataManager/F10/StockDetail 导出用 React.memo 包裹 → tsc 通过
12. **信号平仓未询问退出价格** → Signals.tsx handleCloseSignal 弹出 prompt 输入 exitPrice → tsc 通过
13. **market/overview 缓存过短、数据滞后无提示** → quote.py 缓存 TTL 60s→300s，返回 data_fresh/stale_days/note → 后端已重启验证

## 新增的功能

- 全局 ErrorBoundary 组件（App.tsx）
- 404 页面返回首页入口（App.tsx）

## 性能优化

1. 大盘概览缓存 TTL 从 60 秒延长至 300 秒，减少重复慢查询。
2. 多个页面组件包裹 React.memo，降低不必要重渲染。

## 未修复/未实现的问题（说明原因）

1. **sectors stock_count 全 0** → 缺少板块成分股数据源（通达信 blocknew 无行业文件，需接入 akshare/东方财富）
2. **F10 财务字段 null** → mootdx F10 稀疏/akshare 降级失败，需接入 Guosen financial skill 或 akshare 财务三表
3. **K 线滞后 4 天** → 本地 TDX 数据未更新，需数据自动更新或接入 Guosen 历史行情
4. **signals 列表为空** → 数据库无信号，需启动默认扫描或定时扫描任务
5. **hotspots 空壳数据** → 数据源未接入真实板块行情
6. **health failures 计数 668** → 历史累积计数器，未重置

## 新引入的问题

- 迭代代理中 `ux_stock_list_names` 规则尝试匹配旧版 data.py 代码失败，属于规则与当前代码不匹配，已记录待更新规则库。

## 下一步建议

- 接入 akshare `stock_board_industry_cons_em` / `stock_board_concept_cons_em` 填充 sectors stock_count。
- 接入 Guosen financial skill 替换 mootdx F10，修复财务字段。
- 配置 TDX 数据自动更新或启用 Guosen past_hq 补最近行情。
- 在 backend/main.py 启动事件中增加默认信号扫描（如扫描沪深 300 成分股）。
- 为 market/sentiment 增加首次调用 2 秒超时和缓存预加热，进一步降低大盘接口延迟。
